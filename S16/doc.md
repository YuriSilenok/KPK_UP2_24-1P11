### Вариант №16. Сервис корпусов (Campus)

#### Добавление Campus

Информация требуемая для создания Campus представлена в виде таблицы:

| Параметр | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|----------------|-----|-------------|----------------------|
| name | Да | string | Уникальное, 3-50 символов | — |
| address | Да | string | Не пустое | — |
| floors | Да | integer | Больше 0 | — |

Уникальные комбинации параметров: Отсутствуют. Идентификатор id генерируется автоматически.

Информация возвращаемая в случае удачного создания Campus:

| Параметр | Тип |
|----------|-----|
| id | integer |
| name | string |
| address | string |
| floors | integer |
| is_active | boolean |

---

#### Изменение Campus по ID

Информация требуемая для изменения Campus по ID представлена в виде таблицы:

| Параметр | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|----------------|-----|-------------|----------------------|
| name | Нет | string | Уникальное, 3-50 символов | — |
| address | Нет | string | Не пустое | — |
| floors | Нет | integer | Больше 0 | — |

Информация возвращаемая в случае удачного изменения Campus:

| Параметр | Тип |
|----------|-----|
| id | integer |
| name | string |
| address | string |
| floors | integer |
| is_active | boolean |

---

#### Удаление Campus по ID

Вернет True, если Campus была закрыта (удалена), иначе вернет False

---

#### Получить Campus по ID

Информация возвращаемая в случае удачного поиска Campus по ID:

| Параметр | Тип |
|----------|-----|
| id | integer |
| name | string |
| address | string |
| floors | integer |
| is_active | boolean |

---

#### Получить список Campus по заданным параметрам

Информация требуемая для получения списка Campus:

| Параметр | Тип | Описание |
|----------|-----|----------|
| min_floors | integer | Фильтр по минимальному количеству этажей |
| address_contains | string | Поиск по частичному совпадению адреса |

Информация возвращается в виде списка Campus:

| Параметр | Тип |
|----------|-----|
| id | integer |
| name | string |
| address | string |
| floors | integer |
| is_active | boolean |

---

### Работа с удобствами (Amenity)

#### Добавление Amenity

Информация требуемая для создания Amenity представлена в виде таблицы:

| Параметр | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|----------------|-----|-------------|----------------------|
| title | Да | string | Уникальное, не пустое, до 100 символов | — |

Уникальные комбинации параметров: Отсутствуют. Идентификатор id генерируется автоматически.

Информация возвращаемая в случае удачного создания Amenity:

| Параметр | Тип |
|----------|-----|
| id | integer |
| title | string |

---

#### Изменение Amenity по ID

Информация требуемая для изменения Amenity по ID представлена в виде таблицы:

| Параметр | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|----------------|-----|-------------|----------------------|
| title | Нет | string | Уникальное, не пустое, до 100 символов | — |

Информация возвращаемая в случае удачного изменения Amenity:

| Параметр | Тип |
|----------|-----|
| id | integer |
| title | string |

---

#### Удаление Amenity по ID

Вернет True, если Amenity была закрыта (удалена), иначе вернет False

---

#### Получить Amenity по ID

Информация возвращаемая в случае удачного поиска Amenity по ID:

| Параметр | Тип |
|----------|-----|
| id | integer |
| title | string |

---

#### Получить список Amenity

Информация возвращаемая в случае удачного поиска списка Amenity:

| Параметр | Тип |
|----------|-----|
| id | integer |
| title | string |

---

### ER-диаграмма

```mermaid
erDiagram
    CAMPUS ||--o{ CAMPUS_AMENITY : has
    AMENITY ||--o{ CAMPUS_AMENITY : used_in
    CAMPUS {
        int id PK
        string name
        string address
        int floors
        boolean is_active
    }
    AMENITY {
        int id PK
        string title
    }
    CAMPUS_AMENITY {
        int campus_id FK
        int amenity_id FK
    }