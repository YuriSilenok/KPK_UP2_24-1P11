# Вариант №18. Сервис оборудования аудиторий (Room Equipment Service)

## Добавить оборудование

### Информация, требуемая для создания оборудования

| Параметр | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|---------------|-----|-------------|----------------------|
| name | Обязательно | Строка | 1..100 символов | – |
| type_id | Обязательно | Целое | > 0, ссылка на id в таблице equipment_types | – |
| room_id | Обязательно | Целое | > 0 | – |
| status_id | Необязательно | Целое | > 0, ссылка на id в таблице equipment_statuses | 1 (active) |
| inventory_number | Необязательно | Строка | Уникальный, до 50 символов | NULL |
| description | Необязательно | Строка | До 500 символов | "" |

**Уникальные комбинации параметров:** 
- `inventory_number` (уникальный)
- `room_id` + `name` (в одной аудитории не может быть двух одинаковых названий оборудования)

### Выходные данные

| Параметр | Тип |
|----------|-----|
| id | Целое |
| name | Строка |
| type_id | Целое |
| room_id | Целое |
| status_id | Целое |
| inventory_number | Строка |
| description | Строка |

---

## Изменить оборудование по ID

### Входные параметры

| Параметр | Обязательность | Тип | Ограничение | Значение по умолчанию |
|----------|---------------|-----|-------------|----------------------|
| name | Необязательно | Строка | 1..100 символов | – |
| type_id | Необязательно | Целое | > 0, ссылка на id в таблице equipment_types | – |
| room_id | Необязательно | Целое | > 0 | – |
| status_id | Необязательно | Целое | > 0, ссылка на id в таблице equipment_statuses | – |
| inventory_number | Необязательно | Строка | Уникальный, до 50 символов, может быть NULL | – |
| description | Необязательно | Строка | До 500 символов | – |

### Выходные данные

| Параметр | Тип |
|----------|-----|
| id | Целое |
| name | Строка |
| type_id | Целое |
| room_id | Целое |
| status_id | Целое |
| inventory_number | Строка |
| description | Строка |

---

## Удалить оборудование по ID

### Выходные данные

| Параметр | Тип | Описание |
|----------|-----|----------|
| result | Логический | True – если запись удалена, False – если запись не найдена |

---

## Получить оборудование по ID

### Выходные данные

| Параметр | Тип |
|----------|-----|
| id | Целое |
| name | Строка |
| type_id | Целое |
| room_id | Целое |
| status_id | Целое |
| inventory_number | Строка |
| description | Строка |

---

## Получить список оборудования по заданным параметрам

### Входные параметры

| Параметр | Тип | Описание |
|----------|-----|----------|
| room_id | Целое | Фильтр по ID аудитории |
| type_id | Целое | Фильтр по ID типа оборудования |
| status_id | Целое | Фильтр по ID статуса оборудования |
| search | Строка | Поиск по названию или инвентарному номеру |

### Выходные данные (список)

| Параметр | Тип |
|----------|-----|
| id | Целое |
| name | Строка |
| type_id | Целое |
| room_id | Целое |
| status_id | Целое |
| inventory_number | Строка |
| description | Строка |

---

## Дополнительные таблицы

### Таблица equipment_statuses

| Параметр | Тип | Ограничение | Значение по умолчанию |
|----------|-----|-------------|----------------------|
| id | Целое | PRIMARY KEY, AUTO_INCREMENT | – |
| name | Строка | Уникальный, 1..50 символов | – |
| description | Строка | До 200 символов | "" |

**Начальные данные:**
| id | name | description |
|----|------|-------------|
| 1 | active | Оборудование работает исправно |
| 2 | broken | Оборудование сломано, требует ремонта |
| 3 | maintenance | Оборудование на обслуживании/ремонте |

### Таблица equipment_types

| Параметр | Тип | Ограничение | Значение по умолчанию |
|----------|-----|-------------|----------------------|
| id | Целое | PRIMARY KEY, AUTO_INCREMENT | – |
| name | Строка | Уникальный, 1..100 символов | – |

**Начальные данные:**
| id | name |
|----|------|
| 1 | projector |
| 2 | computers |
| 3 | machines |
| 4 | boards |
| 5 | other |

---

## ER-диаграмма
![Диаграмма](erd.png)